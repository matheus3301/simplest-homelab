apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: kiali
  namespace: argocd
  finalizers:
    - resources-finalizer.argocd.argoproj.io
spec:
  project: services
  source:
    repoURL: https://kiali.org/helm-charts
    chart: kiali-server
    targetRevision: 2.4.0
    helm:
      values: |
        istio_namespace: istio-system
        auth:
          strategy: anonymous
        deployment:
          accessible_namespaces:
            - "**"
          ingress:
            enabled: false
        external_services:
          prometheus:
            url: "http://vmsingle-victoria-metrics-k8s-stack.monitoring.svc.cluster.local:8429"
          grafana:
            enabled: true
            in_cluster_url: "http://victoria-metrics-k8s-stack-grafana.monitoring.svc.cluster.local:80"
            url: "https://monitoring.mmonteiro.dev"
  destination:
    server: https://kubernetes.default.svc
    namespace: monitoring
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
