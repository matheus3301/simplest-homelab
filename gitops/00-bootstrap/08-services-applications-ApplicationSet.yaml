apiVersion: argoproj.io/v1alpha1
kind: ApplicationSet
metadata:
  name: services-applications
  namespace: argocd
spec:
  goTemplate: true
  goTemplateOptions: ["missingkey=error"]
  generators:
    - git:
        repoURL: https://github.com/matheus3301/homelab.git
        revision: HEAD
        directories:
          - path: kubernetes/02-services/*
  template:
    metadata:
      name: 'services-{{ .path.basename }}-apps'
      namespace: argocd
      annotations:
        argocd.argoproj.io/sync-wave: "2"
    spec:
      project: services
      source:
        repoURL: https://github.com/matheus3301/homelab.git
        targetRevision: HEAD
        path: '{{ .path.path }}'
        directory:
          include: '*-Application.yaml'
      destination:
        server: https://kubernetes.default.svc
        namespace: argocd
      syncPolicy:
        automated:
          prune: true
          selfHeal: true
