apiVersion: operator.victoriametrics.com/v1beta1
kind: VMServiceScrape
metadata:
  name: tailscale-proxies
  namespace: tailscale
spec:
  endpoints:
    - port: metrics
      path: /metrics
  selector:
    matchLabels:
      tailscale.com/proxy-metrics: "true"
